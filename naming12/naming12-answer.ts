// Consistency is key..

// Also pretty common.
const enum BoolFlags {
    Foo = 'bar'
}

/* Winner according to google style guide */
const enum BoolFlags {
    FOO = 'bar'
}

const enum BOOL_FLAGS {
    FOO = 'bar'
}


const enum BOOL_FLAGS {
    Foo = 'bar'
}
